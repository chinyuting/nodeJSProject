const { Pool } = require('pg');

const dbPool = new Pool({
  user: 'apuser',
  host: 'localhost',
  database: 'todos',
  password: 'koroto',
  port: 5432, // Default PostgreSQL port
});

module.exports = dbPool;
